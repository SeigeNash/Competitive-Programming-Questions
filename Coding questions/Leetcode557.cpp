#include <iostream>
#include <string>
#include <array>
#include <vector>

using  namespace std;


int main(){

    const char* str = "Hello";
    const char* ptr;
    string temp = "";
    // string space = " ";

    // calculating the number of chars in string 

    int size = 0;

    // pointers to the rescue 

    while( strcmp){
        size += 1;
        ptr += 1;
    }

    for (int i = size; i >= 0 ; i-- ){
        
        temp = temp + str[i];
    }

    cout << temp << endl;
    
    return 0;
}